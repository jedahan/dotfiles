# use with [smug](https://github.com/ivaaaan/smug)
#
#   smug flow

session: flow

root: ~/work/landing-gear/peacock/flow

windows:
  - name: server-logs
    commands:
      - ssh ballthankyou -t ". ~/.bashrc; pm2 logs"
  - name: processor-logs
    commands:
      - ssh cheer-processor -t ". ~/.bashrc; pm2 logs"
  - name: code
    commands:
      - nvim server.ts processor.ts
  - name: git
    commands:
      - git status
