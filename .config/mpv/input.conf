f no-osd set fs-screen 0; cycle fullscreen
g no-osd set fs-screen 1; cycle fullscreen
h no-osd set fs-screen 2; cycle fullscreen
del playlist-remove current ; run "/bin/sh" "-c" "mkdir -p ./.trash" ; run "/bin/sh" "-c" "mv -- '${path}' ./.trash" ; show-text "moved ${path} to trash"
